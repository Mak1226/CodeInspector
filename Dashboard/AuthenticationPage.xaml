<Page x:Class="Dashboard.AuthenticationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dashboard"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1"
      Background="#9EDDFF" 
      Height="500" 
      Width="800">

    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Code Inspector" FontSize="20" Margin="0 0 0 20" HorizontalAlignment="Center"/>

            <!-- Your content for the upper half goes here -->

            <!-- Empty space to push content to the upper half -->
            <StackPanel Height="50" />

            <!--<Button Content="Login with Google" Width="150" Height="40" HorizontalAlignment="Center" Command="{Binding AuthenticateButton_ClickCommand}"/>-->
            <Button Content="Login with Google" Width="150" Height="40" HorizontalAlignment="Center" Click="Login_Click"/>
        </StackPanel>
    </Grid>
</Page>
